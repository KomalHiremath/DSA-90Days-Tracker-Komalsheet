#include <bits/stdc++.h>
using namespace std;
int main(){
    int arr[5];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4]; // 0 1 2 3 4 are the indices of the array
    cout << arr[1];
    return 0;
}
 